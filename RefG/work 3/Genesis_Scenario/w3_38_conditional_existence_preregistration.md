# W3-38 Preregistration: Conditional Genesis Existence Witness

**CLAIM_ID:** `W3_38_CONDITIONAL_GENESIS_EXISTENCE`

**CLAIM:** Given, without deriving, a finite periodic relational graph, a
translation-covariant finite-range tail-bridge rule, and a normalized
accounting budget, at least one configuration exists whose bridged network is
connected and wraps every periodic direction, has no unique graph-theoretic
center, and admits an exact nonnegative locked/tail/unresolved energy ledger.

**TYPE:** `EXACT_CONDITIONAL_EXISTENCE_AND_ACCOUNTING_WITNESS`. This is a
finite mathematical nonemptiness result. It is not a derivation of physical
topology, Genesis dynamics, percolation in nature, or energy partition.

**MODEL_VERSION:** `W3-38-v1.0-CONDITIONAL-EXISTENCE`. Any change to the
periodic fixture, tail rule, finite-event definition, probability calibration,
energy ledger, flag sets, or scope creates a new version.

**ASSUMPTIONS:**

1. The witness graph is the discrete two-torus
   `V=(Z/5Z)^2` with undirected nearest-neighbour edges.
2. Graph labels are bookkeeping labels, not pre-existing physical positions.
3. Every edge has unit bookkeeping length and both endpoint tails have the
   frozen rational extent `rho=3/5`; a bridge is declared iff `2 rho>=1`.
4. Finite percolation means graph connectivity plus an explicit winding cycle
   in each periodic direction. It does not mean an infinite-volume transition.
5. An auxiliary iid bond probability `q=1/2` is used only to prove that the
   finite event has positive measure; `q` is not derived or fitted.
6. The accounting budget is `E_*=1`, with locked amount `1/(4N)` per vertex
   and tail amount `1/(4M)` per edge. These are labels, not physical energies.

**DOMAIN:** The exact finite fixture has `L=5`, `d=2`, `N=25`, and `M=50`.
Probability is finite iid bond bookkeeping. No continuum, thermodynamic limit,
cosmic topology, metric distance, pre-geometric action, or observational domain
is asserted.

**CONVENTIONS:** Translation is addition modulo five. An undirected edge is an
unordered pair. `wrap` means a closed projected walk whose lifted displacement
is five lattice units in the declared periodic direction. `Center(G)=V` means
there is no unique distinguished graph center. Tail overlap is a support rule,
not an amplitude, phase, speed, or PDE. All arithmetic is integer or rational.

**FREEDOM_LEDGER:** Current fitted effective dimension is `d_eff=0`; all
physical freedoms are open and uninstantiated:

- `foundation_relational_support`: source = foundation action; domain = first
  relational state; scale = `universal`; effective_complexity_measure = one
  functional coupling kernel and topology class until derived.
- `dimension_period_topology`: source = global state law; domain = relational
  support; scale = `universal`; effective_complexity_measure = discrete
  dimension, period, and topology indices; the 5x5 torus is only a witness.
- `tail_profile_and_bridge_law`: source = oscillon field equation; domain =
  each tail family; scale = `group`; effective_complexity_measure = one
  profile, phase rule, and bridge functional per family.
- `local_seed_configuration`: source = nonlinear localization; domain = each
  seed; scale = `object`; effective_complexity_measure = one state/profile per
  object.
- `future_connectivity_inference`: source = future likelihood; domain = each
  datum; scale = `data`; effective_complexity_measure = `N_nuisance=0` here.

**DEPENDENCIES:** None. W3-38 is self-contained and does not import W3-37 or a
manuscript. Its assumptions are repeated explicitly.

**METHOD:** Exact integer graph construction, independent BFS, exact winding
walks, exhaustive five-edge ring enumeration, rational probability and energy
arithmetic, translation mutations, exact schema keysets, strict atomic JSON,
and external SHA-256. This method is selected because the claim is finite
existence, not parameter estimation.

**PASS_CONDITION:** The torus counts are exact; every tail bridge is active;
the fixture is connected and wraps both directions; translations are free,
transitive automorphisms; all eccentricities agree and the center set is all
vertices; the finite event has a strictly positive exact probability lower
bound; the ring calibration and energy ledger are exact; every mutation is
detected; all physical flags remain false; aggregate is the AND of the checks.

**FAIL_CONDITION:** Any exact witness fails; a finite wrapping event is called
an infinite-volume transition; the graph or accounting choices are promoted
to physical derivations; a preferred origin is inserted into the tail rule;
or any physical closure flag becomes true.

**FALSIFIER:** A counterexample under the frozen fixture falsifies this
conditional existence gate only. It does not falsify RefG or establish nature's
topology.

**RESIDUAL:** Exact Boolean predicates and rational equalities. Energy balance
requires an exactly zero rational residual.

**ERROR_BOUND:** Zero arithmetic error. Sampling, numerical, and observational
errors are N/A.

**VALIDITY_HEALTH:** PASS proves that one conditional finite construction is
mathematically nonempty. Physical realization and all cosmic interpretations
remain open.

**BRANCHES:** `FULL_PERIODIC_WITNESS`, `LOW_RANGE_MUTATION`,
`SEAM_REMOVAL_MUTATION`, `OPEN_GRID_MUTATION`,
`ORIGIN_SELECTED_RULE_MUTATION`, `ENERGY_OVERRUN_MUTATION`,
`DOUBLE_COUNTED_TAIL_MUTATION`, `ZERO_PROBABILITY_MUTATION`.

**OBSERVABLE_MAP:** N/A. Graph labels and bookkeeping energy are not mapped to
redshift, distance, clocks, temperature, or structure.

**FORWARD_MODEL:** N/A. No physical realization or likelihood is produced.

**DATA_ROLE:** No data are read.

**IDENTIFIABILITY:** The gate identifies only finite combinatorial existence,
translation symmetry, wrapping, and accounting feasibility under supplied
assumptions. It does not identify topology, dimension, tails, probability,
energies, dynamics, or observables.

**BENCHMARK:** `N=25`, `M=50`, `2(3/5)>=1`; full graph BFS size 25;
two explicit winding cycles; `Center(G)=V`; `P(event)>=2^-50`; five-edge ring
returns connected probability `3/16` and wrap probability `1/32`; energy
partition is `1/4+1/4+1/2=1`.

**CLOSURE_FLAGS:**

Exact/computational flags, all required true:

- `finite_graph_counts_exact`
- `tail_bridge_condition_exact`
- `tail_rule_translation_covariant_exact`
- `full_fixture_connected_exact`
- `all_periodic_directions_wrap_exact`
- `translation_action_free_transitive_exact`
- `graph_center_equals_vertex_set_exact`
- `finite_wrapping_event_exact`
- `positive_probability_lower_bound_exact`
- `cycle_bond_enumeration_exact`
- `energy_feasibility_inequality_exact`
- `energy_ledger_balance_exact`
- `energy_ledger_nonnegative_exact`
- `schema_keysets_exact`
- `mutation_controls_pass`
- `aggregate_identity_pass`

Physical flags, all required false:

- `periodic_cosmic_topology_derived`
- `cosmic_dimension_and_period_derived`
- `graph_to_foundation_mapping_derived`
- `tail_field_equation_derived`
- `tail_range_or_speed_derived`
- `bridge_formation_dynamics_derived`
- `occupation_probability_derived`
- `infinite_volume_threshold_derived`
- `physical_universe_percolation_established`
- `cosmological_centerlessness_established`
- `total_genesis_energy_derived`
- `vertex_or_edge_energy_derived`
- `energy_conservation_dynamics_derived`
- `energy_partition_fractions_derived`
- `matter_radiation_assignment_derived`
- `structure_formation_derived`
- `genesis_trigger_derived`
- `observable_forward_model_derived`
- `data_validated`

**CROSSCHECK:** Graph counts by edge-set cardinality; connectivity by BFS;
symmetry by all 25 translations; graph center by all-pairs BFS; ring
probabilities by exhaustive 32-state enumeration; ledger by independent totals.
The tail-energy double-count mutation must make the normalized total unequal
to one.

**PROVENANCE:** The source pins this preregistration's raw SHA-256 and records
its own hash, Python/platform versions, UTC, LF convention, and paths. Strict
finite JSON and its checksum are atomically replaced.

**FILES:** `w3_38_conditional_existence_preregistration.md`,
`w3_38_conditional_existence.py`, `w3_38_result.json`, and
`w3_38_result.sha256`.
