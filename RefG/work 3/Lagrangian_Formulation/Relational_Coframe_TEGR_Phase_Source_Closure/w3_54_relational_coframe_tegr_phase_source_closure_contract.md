# W3-54 — Relational Coframe, TEGR, and Phase-Source Closure

## Target and stopping rule

This stage supplies the smallest post-Genesis continuum foundation law that
realizes the relational RefG ontology and reaches both objects required by the
current program:

1. the Einstein--Hilbert gravitational operator, without placing curvature or
   Einstein's equation in the starting action; and
2. one explicit operational source tensor generated by the retained
   W3-50 neutral phase-density sector.

The stage stops when the selected coframe action is shown to be TEGR, the exact
TEGR--Einstein--Hilbert boundary identity is applied, the phase-current stress
tensor is derived, and the W3-50 and W3-53 overlap checks pass. It does not
open particle profiles, a microscopic equation of state, cosmology, higher
curvature, 2PN, strong-field solutions, or observations.

## 1. Physical state carried by the foundation

The selected connected post-Genesis branch is represented by an oriented,
nondegenerate coframe `e^A_mu`. It is the continuum record of the lengths,
angles, shape, orientation, lapse, and shift of neighboring relational cells.
This is the minimum variable that retains the information which a scalar
pressure field or a Gram determinant alone discards.

With internal metric `eta_AB=diag(-1,1,1,1)`, the one operational metric is

```text
g_mu_nu = eta_AB e^A_mu e^B_nu,       e = det(e^A_mu) > 0.
```

In a selected time gauge on a regular 3+1 chart with `x^0=c0 tau`, write

```text
e^0 = N dx^0,
e^a = E^a_i (dx^i + N^i dx^0),
E = a R exp(s),                       R in SO(3),
s^T=s,                                tr(s)=0.
```

The displayed time gauge is a convenient representative, not a claim that a
general coframe has no boost component. In the local principal-orientation
gauge `R=I`, it reduces to the diagonal witness used by the verifier.

Then

```text
q_ij = delta_ab E^a_i E^b_j = a^2 [exp(2s)]_ij,
sqrt(q)=det(E)=a^3,
e=N sqrt(q).
```

This exactly embeds W3-42's selected three-dimensional volume/shape branch
while retaining the orientation and time-leg information needed for a full
metric. It does not reverse W3-42's nonselection theorem: the relational
graph, three spatial dimensions, smooth coframe phase, Lorentzian time leg,
and continuum limit are selected foundation-law premises here, not derived
from the earlier Gram witness.

Neighboring frames are compared by a flat, metric-compatible inertial
transport `omega^A_Bmu`. On a regular simply connected patch it is restricted
to the inertial orbit `omega=Lambda^(-1)dLambda`, with allowed variations
`delta omega=D lambda`:

```text
R^A_Bmu_nu(omega)=0,                  D_mu eta_AB=0.
```

Their local closure mismatch is the torsion

```text
T^A_mu_nu = 2 partial_[mu e^A_nu]
            + 2 omega^A_B[mu e^B_nu].
```

This mismatch is the mathematical form of the foundation's nonintegrable
relational deformation. Flat inertial transport does not mean a flat
operational metric: the coframe may have nonzero torsion and the associated
Levi-Civita geometry may have nonzero curvature.

The linear selector below is evaluated in the regular Weitzenbock gauge
`omega=0`. In another inertial gauge, `b_mu_nu` denotes the corresponding
gauge-invariant coframe--inertial-connection orientation combination rather
than the antisymmetric tetrad perturbation alone.

## 2. The single selected master action

At leading local, reversible, parity-even, two-derivative order, with constant
gravitational coefficients and no retained torsion--current, torsion--density,
or other nonminimal mixed operator, the three independent quadratic torsion
invariants are

```text
I1 = T^rho_mu_nu T_rho^mu_nu,
I2 = T^rho_mu_nu T^nu_mu_rho,
I3 = T_rho T^rho,                     T_mu=T^nu_nu_mu.
```

Before any coefficient is fixed, define

```text
T_c = c1 I1 + c2 I2 + c3 I3.
```

The scope-bounded selected post-Genesis master action is

```text
S_F[e,omega,J,theta_C]
  = -K_F integral d^4x e (T_c + 2 Lambda_F)
    + integral d^4x [J^mu partial_mu theta_C
                     - (e/c0) rho_C(n_C)],

n_C = sqrt(-g_mu_nu J^mu J^nu)/e > 0,
u^mu = J^mu/(e n_C),                  u^mu u_mu=-1.
```

`J^mu` is a vector density. The first line is the relational-frame sector;
the second is the covariant isentropic completion of W3-50's positive
phase-density pair. They are two state components of one foundation action,
not two gravitational media. The action contains no Levi-Civita curvature,
Einstein tensor, target potential, PPN parameter, or observed profile.

`rho_C(n_C)` is one universal constitutive energy density for the barotropic,
isentropic, irrotational one-potential subfamily selected here. It is positive,
differentiable, and restricted to the stable causal branch
`rho_C'>0` and `0 <= n_C rho_C''/rho_C' <= 1`. Its microscopic form is not
selected here. A constant vacuum offset is assigned only to `Lambda_F` and
is not counted again inside `rho_C`.

## 3. Independent selection of the TEGR coefficients

Choose the regular Weitzenbock gauge on the Minkowski selector patch and
linearize the coframe,

```text
e^A_mu = delta^A_mu + A^A_mu,
h_mu_nu = 2 A_(mu_nu),
b_mu_nu = 2 A_[mu_nu].
```

`h_mu_nu` is the metric perturbation. `b_mu_nu` is the relative-orientation
sector which the metric does not contain. RefG selects one geometry and no
independent long-range orientation field. Therefore the coframe action must
remove both the linear `h-b` mixing and the independent `b` kinetic equation:

```text
2 c1 + c2 + c3 = 0,
2 c1 - c2      = 0.
```

These equations contain no Einstein--Hilbert or Fierz--Pauli coefficient.
Their coefficient matrix has rank two and a one-dimensional nullspace:

```text
(c1,c2,c3) proportional to (1,2,-4).
```

Fixing the conventional normalization while leaving the measured overall
stiffness `K_F` free gives

```text
(c1,c2,c3) = (1/4,1/2,-1),
T_c = T_TEGR = (1/4)I1 + (1/2)I2 - I3.
```

The remaining antisymmetric divergence coefficient
`2c1-3c2-c3` then vanishes automatically. The cited New General Relativity
cubic analysis supplies a separate Minkowski-background obstruction result:
within the local parity-even quadratic-torsion class studied there, the
non-TEGR two-form symmetry of the linear branch does not extend to the
calculated cubic interactions. The paper's result is not promoted to a
universal theorem for every background. At the TEGR point the physical
orientation sector is absent and the remaining action is exactly equivalent
to GR.

Local Lorentz covariance by itself is not used as the selector. In the
covariant coframe-plus-flat-spin-connection formulation generic NGR can be
written locally Lorentz covariantly. The stronger physical condition used
here is that local frame orientation has no independent propagating
foundation state. The selected TEGR endpoint then carries the standard
massless tensor sector.

## 4. Exact conversion to Einstein--Hilbert geometry

The flat coframe connection and the Levi-Civita connection of `g_mu_nu` obey

```text
Gamma = Gamma_LC + K,
```

where `K` is the contortion determined algebraically by torsion. Expanding
the exact identity `R(Gamma)=0`, contracting its two curvature indices, and
collecting the contortion square and divergence gives

```text
R_LC = -T_TEGR + (2/e) partial_mu(e T^mu).
```

This is a connection identity, not a field equation and not a fitted
equivalence. Consequently

```text
- integral e T_TEGR
  = integral e R_LC - 2 integral partial_mu(e T^mu).
```

Set the remaining normalization

```text
K_F = c0^3/(16 pi G).
```

`G` is the measured universal stiffness; its numerical value is not derived.
For compact-support variations with appropriate asymptotic falloff, or after
adding the explicitly matched boundary functional, the master action becomes

```text
S_F = c0^3/(16 pi G) integral d^4x e (R_LC-2 Lambda_F)
      + S_C[g,J,theta_C] + S_boundary.
```

The quadratic symmetric part provides an independent regression. Modulo a
boundary term, the TEGR invariant has the coefficient registry

```text
T_TEGR^(2) = (1/4) A - (1/2) B + (1/2) C - (1/4) D,
```

for

```text
A=(partial h_mu_nu)^2,
B=(partial_mu h^mu_nu)^2,
C=(partial_mu h^mu_nu)(partial_nu h),
D=(partial h)^2.
```

Thus `-2 T_TEGR^(2)` has exactly the W3-53 Fierz--Pauli registry
`(-1/2,1,-1,1/2)`. The spin-two operator is therefore an output of the
selected coframe action, not a second premise added to it.

## 5. The source tensor from the same action

Varying `theta_C` gives the exact internal phase current law

```text
partial_mu J^mu = 0.
```

Varying `J^mu` gives

```text
partial_mu theta_C + [rho_C'(n_C)/c0] u_mu = 0.
```

Holding `J^mu` fixed during metric variation gives

```text
delta n_C
  = (n_C/2)(g_mu_nu+u_mu u_nu) delta g^mu_nu.
```

Define

```text
p_C = n_C rho_C'(n_C)-rho_C(n_C).
```

Then

```text
delta S_C
  = -(1/(2c0)) integral d^4x e T^C_mu_nu delta g^mu_nu,

T^C_mu_nu
  = [rho_C(n_C)+p_C(n_C)] u_mu u_nu
    + p_C(n_C) g_mu_nu.
```

The source has therefore not been named by analogy: it is the Hilbert tensor
obtained by varying the retained foundation phase-current action. In its rest
frame it has `T_00=rho_C`, `T_0i=0`, and `T_ij=p_C delta_ij`. Diffeomorphism
invariance gives `nabla_mu T_C^mu_nu=0` on the phase-current equations.

Varying the complete action gives

```text
G_mu_nu + Lambda_F g_mu_nu
  = (8 pi G/c0^4) T^C_mu_nu.
```

Foundation geometry is kept on the left-hand side and the retained organized
phase excitation is counted once on the right. Metric self-energy, `P_F`, the
readout `p`, material size/mass/cadence changes, and a second vacuum offset
are not added as extra sources.

`p_C` is the thermodynamic pressure of the selected phase-current action.
W3-46's foundation pressure readout `P_F` remains a distinct symbol. No
identity `p_C=P_F` is asserted without a future constitutive derivation.

## 6. Exact W3-50 reduction

On the lapse-one, shift-zero homogeneous comoving branch,

```text
J^0=e n_C,             J^i=0,          sqrt(q)=a^3.
```

Using `x^0=c0 tau`, the phase action reduces to

```text
S_C = integral d tau d^3x sqrt(q)
      [n_C partial_tau theta_C-rho_C(n_C)].
```

This is W3-50's canonical action on the instantiated barotropic, isentropic,
irrotational one-potential subfamily `H_C=rho_C(n_C)`. The exact current law
becomes

```text
d(a^3 n_C)/d tau=0.
```

With W3-50's definition `eta_F=n_C/n_C0` and `a_0=1`, it follows that

```text
eta_F a^3=1.
```

The new action therefore preserves the already accepted dilution law while
adding the previously missing covariant stress response. It does not derive
the microscopic function `rho_C(n_C)` or identify it with the separate
W3-47 pressure-readout law.

## 7. Frozen claim contract

- `CLAIM_ID`: `W3_54_RELATIONAL_COFRAME_TEGR_PHASE_SOURCE_CLOSURE`.
- `CLAIM`: On the explicitly selected connected post-Genesis relational
  coframe branch and within the frozen constant-coefficient, minimally
  coupled, parity-even leading quadratic-torsion sector, the gravitational
  action has three coefficients. Requiring no independent
  physical orientation sector fixes their ratios uniquely to TEGR. The exact
  flat-connection identity then converts that action to Einstein--Hilbert
  plus a boundary term. The covariant W3-50 phase-current sector in the same
  master action produces an explicit conserved Hilbert tensor and reduces to
  `eta_F a^3=1` on the homogeneous branch.
- `TYPE`:
  `SELECTED_POST_GENESIS_CONTINUUM_FOUNDATION_LAW_WITH_EXACT_DOWNSTREAM_GEOMETRY_AND_SOURCE_DERIVATION`.
- `MODEL_VERSION`:
  `W3-54-v1.0-RELATIONAL-COFRAME-TEGR-PHASE-SOURCE-CLOSURE`. A change to the
  coframe state, flat-transport rule, derivative/parity truncation,
  orientation-sector selector, phase-current action, source ledger,
  signature, or boundary convention creates a new model version.
- `EVIDENCE_STATUS`:
  `CONDITIONAL_EXACT_SELECTED_RELATIONAL_COFRAME_MASTER_ACTION_TO_TEGR_EQUIVALENT_EH_AND_PHASE_CURRENT_T`
  only when every algebraic, dependency, mutation, and regression gate passes.

### ASSUMPTIONS

1. A selected connected, oriented, time-oriented, nondegenerate smooth `3+1`
   post-Genesis coframe phase exists and embeds W3-42's spatial branch.
2. The internal metric is Lorentzian and all retained operational excitations
   couple to the same coframe metric.
3. Frame transport is flat and metric-compatible; its local closure mismatch
   is torsion. On each regular simply connected patch
   `omega=Lambda^(-1)dLambda`, allowed variations preserve flatness, and the
   linear selector uses Weitzenbock gauge or the equivalent gauge-invariant
   coframe--connection orientation combination.
4. The leading foundation action is local, reversible, parity even, and
   quadratic in torsion, with constant `c_i`, minimal phase coupling, and no
   retained torsion--current, torsion--density, or other mixed nonminimal
   operator; higher-gradient operators are cutoff-suppressed.
5. Local frame orientation is not an independent physical long-range state.
   The cited non-TEGR obstruction is used only on its calculated Minkowski
   cubic domain; TEGR equivalence supplies the retained massless tensor sector.
6. The retained organized content is W3-50's positive shift-symmetric phase
   density, completed covariantly as a barotropic, isentropic, irrotational
   one-potential flow with one stable causal constitutive function
   `rho_C(n_C)` and minimal coupling to the same coframe.
7. Variations have compact support and suitable asymptotic falloff, or the
   explicitly matched boundary functional is retained.
8. One vacuum coefficient occupies `Lambda_F`; the source ledger contains no
   duplicate pressure, metric self-energy, readout, or vacuum entry.

### DOMAIN

Connected nondegenerate post-Genesis continuum states; smooth oriented
coframes; regular flat inertial transport; leading parity-even quadratic
torsion order; positive barotropic/isentropic irrotational phase flow; stable
causal constitutive branch; boundary data compatible with the variational
identity. Excluded are
the Genesis degeneracy, graph selection, discrete-to-continuum error bounds,
topology-changing frames, parity-odd and higher-gradient operators,
microscopic oscillon profiles, explicit particle species, EOS derivation,
parameter matching, strong-field solutions, and observations.

### CONVENTIONS

Internal indices `A,B=0,...,3`, spacetime indices `mu,nu=0,...,3`, and
spatial indices `a,i=1,2,3`. The derivation uses signature `(-+++)` and
`x^0=c0 tau`. The Riemann sign is
`R^rho_sigma_mu_nu=partial_mu Gamma^rho_nu_sigma-partial_nu Gamma^rho_mu_sigma+...`.
Torsion is defined directly by the displayed coframe formula; spacetime
indices are raised with `g_mu_nu`, internal indices with `eta_AB`.
`omega_AB=-omega_BA`, `R(omega)=0`, and the linear selector uses
Weitzenbock gauge `omega=0` on its regular patch. `e=det(e^A_mu)>0`.
`J^mu` is a weight-one vector density. The Hilbert convention is
`T_mu_nu=-(2c0/e) delta S_C/delta g^mu_nu`. Coordinates have length units;
the coframe and metric are dimensionless, `[T]=L^(-1)`, and
`[R_LC]=[Lambda_F]=L^(-2)`. `rho_C` has energy-density units and `theta_C` is
dimensionless. `a` is relational link dilation;
`eta_F` is normalized phase-action density; `P_F` is the foundation pressure
readout; and `p_C` is phase-sector thermodynamic pressure. These symbols are
not silently identified. Reversing the overall metric signature changes the
presentation, not the physical content.

### FREEDOM_LEDGER

- fitted data parameters and fitted functions: zero;
- `G`: one measured universal normalization, not derived here;
- `Lambda_F`: one universal zero-derivative coefficient, value not derived;
- `rho_C(n_C)`: one infinite-dimensional universal functional freedom on
  `n_C>0`, at least `C^2`, constrained by `rho_C>0`, `rho_C'>0`, and
  `0<=n_C rho_C''/rho_C'<=1`; selected but not fitted or microscopically
  derived;
- cutoff/higher-gradient coefficients: not opened and not derived;
- selected structural premises: `3+1` coframe continuum, Lorentzian internal
  metric, flat inertial transport, leading quadratic-torsion truncation, no
  physical orientation mode, constant gravitational coefficients, absence of
  retained mixed torsion--phase operators, one common operational geometry,
  and one source ledger;
- object-specific couplings, target PPN values, profiles, interpolation
  functions, second metrics, and duplicate sources: none.

### DEPENDENCIES

Hash-locked logical inputs:

- W3-42 foundation state-space/volume-map preregistration plus its pinned
  result: `status=PASS`, `aggregate_identity_pass=true`, and
  `three_spatial_dimensions_derived=false`;
- W3-46 active-participation/resonance-feedback contract, including
  `participation_meaning_selected=true`,
  `full_state_and_symmetry_derived=false`, and
  `master_action_or_resonance_PDE_derived=false`;
- W3-50 neutral collective phase-density bridge contract, including its
  `PASS_EXACT_CONDITIONAL...MASTER_FOUNDATION_ORIGIN_OPEN` status, exact
  `eta_a3` charge flag, and false physical-dimension/complete-Hamiltonian
  boundaries.

W3-53 and W3-52 are downstream regressions, not inputs. Work 2, RefG-GR, and
the historical ansatz scripts are neither evidence nor dependencies.

### METHOD

Exact SymPy determinant and coefficient-matrix algebra; independent solution
of the orientation-sector constraints; antisymmetric-sector mutation tests;
quadratic TEGR-to-Fierz--Pauli coefficient comparison; flat-FLRW sign witness
for the torsion--curvature identity; exact phase-current metric variation;
rest-frame stress check; exact master-action coupling ratio; homogeneous
W3-50 reduction; source-count audit; dependency hashes plus required upstream
status/closure markers; premise and operator injection mutations; and actual
captured reruns of W3-53, W3-51, and W3-52 recorded in the result JSON.

### PASS_CONDITION

The coframe determinant identities hold; the NGR selector matrix has rank two
and only the TEGR ratio survives; every single-coefficient mutation is
detected; the residual antisymmetric coefficient vanishes; the TEGR quadratic
metric sector equals Fierz--Pauli; the curvature-boundary identity has the
correct sign; the phase variations give current conservation and the stated
Hilbert tensor; its rest-frame components and Einstein coupling are exact;
the homogeneous reduction gives `eta_F a^3=1`; dependencies match; every
forbidden target/operator and duplicate-source mutation fails; and all
downstream regressions pass.

### FAIL_CONDITION

Curvature, the Einstein equation, a target FP coefficient, PPN datum, or
observed profile enters the starting operator list; a non-TEGR coefficient
passes the no-orientation-mode gate; local Lorentz covariance alone is
misreported as the selector; the TEGR identity has the wrong sign; `T_mu_nu`
is named without varying `S_C`; `P_F` is silently renamed `p_C`; a readout or
metric self-energy is counted again as matter; the W3-50 charge law changes;
an upstream hash changes; or any exact/mutation/regression gate fails.

### FALSIFIER

The selected completion fails if the physical post-Genesis spectrum contains
an independent unsuppressed orientation mode, lacks the two-helicity tensor
sector, requires non-flat frame transport at leading order, violates the one
coframe coupling, or cannot support a positive shift-symmetric phase density.
A future node model that does not coarse-grain to the selected coframe action
would falsify its proposed microscopic realization while leaving the exact
conditional TEGR/EH and source identities intact.

### RESIDUAL

Exact symbolic zero for coframe determinant, selector equations,
antisymmetric remainder, TEGR normalization, FP registry, FLRW sign witness,
phase metric-variation coefficients, rest-frame source components, Einstein
coupling, and homogeneous charge law. The general TEGR--EH connection identity,
the NGR classification, and the scoped Minkowski cubic-obstruction result are
analytic/theorem applications, not computed residuals.

### ERROR_BOUND

Zero for exact algebra and file hashes. No numerical approximation or datum
enters. No error bound is assigned to the selected discrete-to-continuum
coframe premise, omitted higher-gradient terms, or the uninstantiated
`rho_C(n_C)` before a microscopic cutoff and EOS are derived.

### VALIDITY_HEALTH

The action is local, reversible, diffeomorphism covariant, one-metric, and
one-source at the retained order. The orientation-sector selector is
independent of the EH target. Stability and causality of the phase sector are
explicit restrictions on `rho_C`; nonlinear tensor health is a registered
TEGR-equivalence consequence, while the non-TEGR comparison uses only the
registered Minkowski cubic-obstruction result. The result establishes an exact post-Genesis
downstream closure on the selected foundation-law branch, not a node-by-node
derivation of why nature picks that branch.

### BRANCHES

- selected: nondegenerate relational coframe, flat inertial transport,
  parity-even quadratic torsion, no physical orientation mode, TEGR,
  positive phase current, one metric/source ledger;
- rejected: pressure-only scalar geometry, pure-gradient elastic
  displacement, generic NGR with a physical two-form/orientation mode,
  species-dependent metric, duplicate pressure source, and hidden EH input;
- deferred: node graph and dimension selection, discrete continuum limit,
  oscillon/EOS microphysics, `P_F`--`p_C` matching, `G`/`Lambda_F` values,
  higher gradients, particles, cosmology, and observations.

### OBSERVABLE_MAP

None added. W3-52 remains the separate inherited 1PN implication on the
resulting Einstein--Hilbert branch.

### FORWARD_MODEL

`N/A`; no instrument, synthetic observation, catalogue, fit, or likelihood.

### DATA_ROLE

`NO_DATA_READ_OR_FITTED`.

### IDENTIFIABILITY

The no-orientation-mode equations identify only the relative torsion
coefficients. The overall stiffness is measured as `G`; `Lambda_F` and
`rho_C(n_C)` remain independent universal inputs. Current evidence does not
identify the node graph, continuum cutoff, microscopic EOS, or pressure
readout bridge.

### BENCHMARK

The selected coefficients must be `(1/4,1/2,-1)`; the exact identity must be
`R_LC=-T_TEGR+2 e^(-1) partial_mu(eT^mu)`; the symmetric quadratic sector must
be W3-53 Fierz--Pauli; the source must be
`T_mu_nu=(rho_C+p_C)u_mu u_nu+p_C g_mu_nu`; and the homogeneous current must
give `eta_F a^3=1`.

### CLOSURE_FLAGS

Required true:

- `SELECTED_POST_GENESIS_RELATIONAL_COFRAME_STATE`
- `W3_42_POLAR_COFRAME_GRAM_WITNESS_EXACT`
- `SELECTED_COFRAME_DEFINES_FULL_OPERATIONAL_METRIC`
- `TIME_GAUGE_METRIC_DETERMINANT_WITNESS_EXACT`
- `FLAT_INERTIAL_TRANSPORT_SELECTED`
- `FLAT_CONNECTION_VARIATION_DOMAIN_SELECTED`
- `GENERAL_PARITY_EVEN_QUADRATIC_TORSION_BASIS_REGISTERED`
- `NO_PHYSICAL_ORIENTATION_SECTOR_SELECTED`
- `TEGR_COEFFICIENT_RATIOS_DERIVED`
- `NGR_MINKOWSKI_CUBIC_OBSTRUCTION_RESULT_APPLIED`
- `TEGR_EH_CONNECTION_IDENTITY_APPLIED_AND_SIGN_CHECKED`
- `TEGR_TO_FIERZ_PAULI_REGRESSION_EXACT`
- `W3_50_IRROTATIONAL_PHASE_CURRENT_COVARIANT_COMPLETION_SELECTED`
- `PHASE_CURRENT_CONSERVATION_DERIVED`
- `PHASE_HILBERT_T_DERIVED`
- `W3_50_ETA_A3_REGRESSION_EXACT`
- `EINSTEIN_EQUATION_FROM_SINGLE_MASTER_ACTION_EXACT`
- `ONE_SOURCE_LEDGER_SELECTED_AND_CONSISTENT`
- `RELATIONAL_COFRAME_TO_EH_AND_PHASE_T_GATE_CLOSED`

Required false beyond the declared closure:

- `PREGEOMETRIC_GRAPH_DERIVED`
- `THREE_SPATIAL_DIMENSIONS_DERIVED`
- `NODE_TO_COFRAME_CONTINUUM_DERIVED`
- `LORENTZIAN_TIME_LEG_FROM_NODES_DERIVED`
- `FLAT_TRANSPORT_FROM_NODE_DYNAMICS_DERIVED`
- `COMMON_CONE_FROM_NODE_SPECTRUM_DERIVED`
- `NO_ORIENTATION_MODE_FROM_NODE_SPECTRUM_DERIVED`
- `COFRAME_PHASE_SPLIT_FROM_NODES_DERIVED`
- `MICROSCOPIC_T_FROM_NODES_DERIVED`
- `RHO_C_FROM_OSCILLON_MICROPHYSICS_DERIVED`
- `P_F_EQUALS_P_C_DERIVED`
- `G_VALUE_DERIVED`
- `LAMBDA_VALUE_DERIVED`
- `HIGHER_GRADIENT_COEFFICIENTS_DERIVED`
- `PARTICLE_PROFILES_DERIVED`
- `NEW_OBSERVATION_TESTED`

### CROSSCHECK

Recover the cubic spatial measure both from `det(E)` and `sqrt(det(q))`;
recover TEGR ratios from the selector nullspace and direct substitution;
verify every one-coefficient mutation; verify the unused antisymmetric
coefficient; reconstruct the W3-53 FP registry from the torsion invariants;
check the TEGR identity on a nontrivial flat-FLRW coframe; derive the Hilbert
variation in the independent `(g_mu_nu,u_mu u_nu)` basis; verify rest-frame
energy and pressure; recover `8piG/c0^4` from the two action variations;
reduce to W3-50 in comoving coordinates; audit the one-source ledger; rerun
W3-53, W3-51, and W3-52 after the new gate.

### PROVENANCE

The verifier pins the exact W3-42, W3-46, and W3-50 contract hashes and its
own contract hash; records Python, SymPy, platform, UTC, exact checks,
mutations, theorem roles, closure flags, and the source ledger; serializes
strict UTF-8 JSON with `allow_nan=False`; and writes the generated result
atomically. The generated JSON is ignored by Git under the repository policy.

### FILES

- `w3_54_relational_coframe_tegr_phase_source_closure_contract.md`
- `w3_54_relational_coframe_tegr_phase_source_closure.py`
- generated `w3_54_result.json`
- synchronized `Lagrangian_Formulation/RefG_Formal_Proof.md`

### STOP_RULE

Stop after the TEGR coefficient selection, exact Einstein--Hilbert boundary
identity, explicit phase-current `T_mu_nu`, W3-50 charge regression, and
bounded W3-53/W3-51/W3-52 regressions. Do not continue into a node graph,
particle construction, EOS search, cosmology, strong field, higher curvature,
or data analysis.

## 8. Evidence roles and primary sources

Computed here after the registered analytic inputs: determinant identities,
selector rank/nullspace, coefficient mutations, antisymmetric remainder,
TEGR-to-FP combination, FLRW sign witness, phase metric variation, rest-frame
tensor, action coupling, and homogeneous charge reduction. Premise,
forbidden-operator, theorem, and source-ledger registries are manifest
integrity checks and are not counted as independent physical derivations.

Exact geometric identity: the flat-connection torsion/Levi-Civita-curvature
boundary relation, derived by connection decomposition and independently
sign-checked by the verifier.

Published/analytic handoffs: classification of the parity-even quadratic NGR
family; the scoped Minkowski cubic obstruction for its non-TEGR two-form
branch; TEGR degree-of-freedom equivalence; the exact connection identity; and
the standard variational potential-flow source construction.

- K. Hayashi and T. Shirafuji, *New General Relativity*, Phys. Rev. D 19,
  3524 (1979), https://doi.org/10.1103/PhysRevD.19.3524.
- D. Blixt, M. Hohmann, and C. Pfeifer, *Hamiltonian and primary constraints
  of new general relativity*, Phys. Rev. D 99, 084025 (2019),
  https://arxiv.org/abs/1811.11137.
- J. Beltran Jimenez and K. F. Dialektopoulos, *Non-Linear Obstructions for
  Consistent New General Relativity*, JCAP 01 (2020) 018,
  https://arxiv.org/abs/1907.10038.
- J. W. Maluf, *The teleparallel equivalent of general relativity*,
  https://arxiv.org/abs/1303.3897.
- J. D. Brown, *Action functionals for relativistic perfect fluids*,
  Class. Quantum Grav. 10 (1993) 1579,
  https://arxiv.org/abs/gr-qc/9304026.

## Decision semantics

Passing W3-54 means that RefG now has one explicit, noncurvature starting
action on its selected post-Genesis relational continuum branch. That action
derives the TEGR coefficient point, is exactly Einstein--Hilbert up to a
boundary, generates one explicit phase-current `T_mu_nu`, and preserves the
accepted homogeneous participation law. It does not claim that the earlier
pregeometric information alone forced the coframe branch or its constitutive
function.
