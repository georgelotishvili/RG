# W3-56 Preregistration: One-Oscillon Coframe-Lock Bridge

**CLAIM_ID:** `W3_56_ONE_OSCILLON_COFRAME_LOCK_BRIDGE`

**CLAIM:** On a positive homogeneous background, a selected minimal quadratic auxiliary response driven by the W3-50 carrier amplitude has one stable branch. Its normalized response is linear in carrier amplitude and its selected positive restoring-channel readout is quadratic. A selected stable proper probe normal form, together with an independent finite localized scaling witness, then realizes coordinate cadence, coordinate radius, and externally read energy/mass proportional to the same factor under the corresponding biconformal pullback, while local proper dynamics is unchanged. Consequently `p^2=P_F^(R)/P_F0^(R)=eta_F` follows exactly inside this fixed-background reduced realization without placing that target relation or a fitted power in the primitive response potential. This result is a consistency witness and candidate architecture; it is not yet a localized W3-50 oscillon-core derivation.

**TYPE:** `EXACT_INTERNAL_ALGEBRA_OF_A_SELECTED_QUADRATIC_AUXILIARY_RESPONSE_AND_BICONFORMAL_PROBE_WITNESS`. This is a concrete effective realization and falsifiable candidate architecture. It is not the localized oscillon derivation demanded by W3-50, is not forced by W3-50/W3-54, and is not a node-derived covariant completion.

**MODEL_VERSION:** `W3-56-v1.0-REDUCED-ONE-OSCILLON-COFRAME-LOCK`.

**ASSUMPTIONS:**

1. W3-50 supplies `Psi_C=B exp(i theta_C)`, `B=|Psi_C|>0`, `n_C=B^2`, and `eta_F=n_C/n_C0=(B/B0)^2` on the homogeneous branch.
2. A positive auxiliary response `b` has the fixed-background reduced potential

   ```text
   U_R(b,B)=K (b-lambda B)^2/2,
   K>0, lambda>0.
   ```

   It contains neither `p` nor `P_F`; it is the lowest bounded Hookean realization of a linear coherent-amplitude response. `B^2` is retained in the completed square, so the reduced sector is bounded below.
3. The candidate pressure readout is selected as the positive restoring-channel intensity `P_F^(R)=K b^2/2`. This is not the total potential, not a derived Hilbert stress, not W3-54's thermodynamic `p_C=n_C rho_C'-rho_C`, and not a second Hilbert source.
4. The reference state has `B0>0`, `b0=lambda B0`, and `P_F0^(R)=K b0^2/2`. Define `beta=b/b0` only after solving the response equation.
5. A stable proper action-angle and transverse normal form `H_0` is assumed as the probe dynamics, while an independent finite localized energy-density profile is used only as a scaling witness. No field equation connects that profile to `H_0`; no finite-energy oscillon solution or Floquet operator is claimed. `H_0` contains no `B`, `b`, `P_F`, `p`, or environmental power law by selected probe construction, not by derivation from W3-50.
6. In the probe/Born--Oppenheimer split, the homogeneous medium first fixes `b`; the one oscillon then propagates on that background without being varied back into the auxiliary background equation. Its canonical action is

   ```text
   S_1osc=integral dt [J_O theta_O_dot+sum(Pi_r Qdot_r)-beta H_0(J_O,Q_r,Pi_r)].
   ```

7. The local probe coframe is `d tau=beta dt`, `d y=d x/beta`, hence `ds^2=beta^2 c0^2 dt^2-beta^(-2)d x^2`. Constant `beta` gives the exact profile pullback. A varying background is used only for homogeneous action-angle reparametrization or at local adiabatic order.

**DOMAIN:** Positive post-Genesis carrier density; fixed-background auxiliary response; one selected stable proper probe normal form; one independent finite localized scaling witness; one homogeneous isotropic patch. Exact profile and coordinate-spectrum statements require constant `beta`; the process-time Hamilton equations remain exact for homogeneous positive `beta(t)`. Localized backreaction, gradients of the response, compact objects, coframe degeneracy, phase unlocking, strong fields, and 2PN are excluded.

**CONVENTIONS:** `t` is coordinate time, `tau` local process/proper time, `x` the coordinate chart, and `y` the local orthonormal spatial coordinate. Coordinate energy generates `t`; proper energy is generated by `tau`. `m_eff=E_t/c0^2` is the stationary external mass readout. Collective phase, oscillon phase, and process time remain distinct. `P_F^(R)` is the selected positive restoring-channel magnitude and is never relabelled as `p_C`.

**FREEDOM_LEDGER:** `K` and `lambda` are two universal positive reduced-response coefficients; they cancel from every normalized result and are not data-fitted. `B(t)` is a background history supplied elsewhere. `H_0` supplies one reference oscillon's absolute energy, radius, and dimensionless spectrum; W3-56 derives only their environmental scaling. Object-specific exponents, pressure floors, transition-specific cadence factors, cosmological parameters, and likelihood nuisances are absent.

**DEPENDENCIES:** W3-50 supplies only `n_C=B^2` and the phase-role distinctions. W3-54 supplies the one-coframe/one-source convention and `P_F != p_C`. W3-51/W3-52 supply the bounded biconformal readout convention. The verifier pins and checks all four contract hashes. The target bridge assumed by W3-40/W3-46/W3-47/W3-55 is not imported as a premise. Work 2, RefG-GR, frequency-to-mass powers, Koide/C3, Planck hierarchy, and data are excluded.

**METHOD:** Verify the pinned upstream meanings; vary the target-free completed-square response; prove its unique positive branch and Hessian; evaluate the selected restoring-channel readout; derive its normalized pressure/density law; reparametrize one positive oscillon action-angle normal form; derive the field-action Jacobian and derivative maps under `tau=beta t`, `y=x/beta`; obtain cadence, spectrum, signal speed, radius, energy, and mass scaling; link a finite localized energy-density witness explicitly to `beta`; prove that the free W3-54 equation-of-state family does not select this exponent; and run every structural mutation through the same normalized validators used by the selected branch.

**PASS_CONDITION:** Every stationary, stability, normalization, proper-time, spectrum, size, energy, mass, signal-speed, finite-profile, health, schema, and mutation check passes; the primitive potential is target-free; all covariant-origin, full-core, backreaction, universality, strong-field, and observational flags remain false.

**FAIL_CONDITION:** Any registered check fails; the target relation is inserted into the primitive potential; `P_F^(R)` is called the total potential, a derived Hilbert stress, or `p_C`; the oscillon is fed back into the auxiliary equilibrium while the probe result is retained; a background-dependent proper mass is introduced; phase roles collapse; or the result is promoted beyond its fixed-background witness domain.

**FALSIFIER:** Inside the reduced model, any nonzero exact residual or negative response/fluctuation coefficient falsifies the claim. The candidate mechanism is physically rejected if the proper oscillon spectrum varies with homogeneous `B`, if one coframe factor fails to control cadence/radius/external energy, if the restoring readout is not quadratic, or if an unavoidable state-independent response floor or species-dependent nonfactorizing term is found.

**RESIDUAL:** Exact symbolic zero for the response equation, normalized bridge, explicit `n_C=B^2` map, process-time Hamilton equations, constant-patch spectral factor, pullback Jacobian and derivative coefficients, coordinate size, energy, mass, and signal-speed maps; exact positivity for the response Hessian and pulled-back kinetic/gradient coefficients; exact finite localized normalization and `beta`-linked rms-radius scaling.

**ERROR_BOUND:** Zero algebraic error inside the frozen reduced model. The profile pullback is exact at constant background. Gradient, tidal, and backreaction corrections are excluded rather than assigned an artificial bound.

**VALIDITY_HEALTH:** Fixed `B` gives a unique bounded auxiliary minimum because `K>0`. Positive `beta` preserves the signs of the selected proper probe Hamiltonian and pulled-back field kinetic and gradient terms. The completed square removes the negative unbounded carrier shift. This is not a stability proof for a localized oscillon solution and does not cover a covariant TEGR embedding, a dynamical joint `B-b` Hessian, or oscillon backreaction.

**BRANCHES:** `POSITIVE_FIXED_BACKGROUND_RESPONSE`, `HOMOGENEOUS_ACTION_ANGLE_EXACT`, `CONSTANT_PATCH_PROFILE_EXACT`, and `SLOW_BACKGROUND_LOCAL_ADIABATIC`. `BACKREACTING_COMPACT_SOURCE`, `UNLOCKED`, `ADDITIVE_FLOOR`, and `NONLINEAR_DRIVE` are outside or negative-control branches.

**OBSERVABLE_MAP:** For this selected fixed-background probe witness,

```text
p=beta=B/B0,
P_F^(R)/P_F0^(R)=eta_F=p^2,
Omega_t/Omega_t0=R_x/R_x0=E_t/E_t0=m_eff/m_eff0=p,
Omega_tau/Omega_tau0=R_y/R_y0=E_tau/E_tau0=1,
c_coord/c_coord0=p^2.
```

No absolute particle property, redshift, distance, or likelihood is generated.

**FORWARD_MODEL:** Input `B/B0>0` and one fixed proper oscillon normal form; output its normalized local coordinate/proper readouts. There is no data fit.

**DATA_ROLE:** No data are read. The finite localized witness is independent of `H_0` and checks only the pullback scaling; it is not a solved oscillon or claimed fundamental particle profile.

**IDENTIFIABILITY:** The half-power is fixed within the chosen operator class: a completed-square linear response gives `b proportional to B`, while the positive restoring component is quadratic in `b`. Normalization cancels `K` and `lambda`. The operator class is selected, not derived or unique; its offset and nonlinear alternatives make different predictions and are explicit mutations.

**BENCHMARK:** At `B/B0=2`, require `p=2`, `P_F^(R)/P_F0^(R)=eta_F=4`, coordinate cadence/radius/external energy/defined-mass ratios `2`, coordinate signal-speed ratio `4`, and unit proper ratios.

**CLOSURE_FLAGS:** Required true: `primitive_response_target_symbols_absent_exact`, `completed_square_bounded_exact`, `positive_stationary_response_exact`, `response_hessian_positive_exact`, `dependency_hashes_pinned_exact`, `density_amplitude_identity_exact`, `selected_restoring_readout_square_law_exact`, `w3_54_eos_underdetermination_exact`, `action_angle_reparametrization_exact`, `process_time_transverse_equations_exact`, `proper_cadence_invariant_exact`, `selected_probe_coordinate_spectrum_factorization_exact`, `dimensionless_spectrum_invariant_exact`, `field_pullback_jacobian_exact`, `biconformal_field_pullback_exact_constant_patch`, `coordinate_signal_speed_square_exact`, `coordinate_radius_linear_exact`, `external_energy_and_defined_mass_linear_exact`, `finite_localized_scaling_witness_exact`, `positive_field_coefficients_exact`, `phase_roles_distinct_exact`, `single_probe_coframe_no_duplicate_source_bookkeeping_registered_exact`, `registered_contract_keysets_exact`, `mutation_controls_pass`, and `aggregate_candidate_pass`. Required false: `selected_operator_from_nodes_derived`, `P_F_readout_from_covariant_stress_derived`, `covariant_TEGR_embedding_derived`, `joint_B_b_dynamics_stable`, `localized_backreaction_derived`, `reference_oscillon_core_from_foundation_derived`, `environment_independence_of_H0_derived`, `finite_energy_oscillon_solution_constructed`, `W3_50_localized_spectral_gate_closed`, `one_source_one_coframe_from_action_derived`, `full_nonlinear_oscillon_PDE_solved`, `all_particle_families_universalized`, `particle_mass_spectrum_derived`, `Koide_or_C3_used`, `Planck_hierarchy_derived`, `strong_field_or_2PN_completed`, `cosmological_history_derived`, and `observational_likelihood_evaluated`.

**CROSSCHECK:** Derive the square relation from the stationary response and restoring-channel normalization; derive cadence from both action-angle mechanics and the field pullback; derive energy from both the canonical Hamiltonian and transformed energy density; verify proper-time cancellation; and detect all declared structural mutations.

**PROVENANCE:** Frozen UTF-8 LF preregistration with pinned SHA-256; pinned SHA-256 records for W3-50, W3-54, W3-51, and W3-52 contracts; deterministic Python/SymPy; runtime source/result hashes; strict finite JSON; atomic result/checksum writes; no network dependency.

**FILES:** `README.md`, `w3_56_one_oscillon_coframe_lock_bridge_preregistration.md`, `w3_56_one_oscillon_coframe_lock_bridge.py`, `w3_56_result.json`, and `w3_56_result.sha256`.

## Decision semantics and stop rule

Successful status:

`PASS_EXACT_INTERNAL_ALGEBRA_OF_SELECTED_FIXED_BACKGROUND_RESPONSE_AND_BICONFORMAL_PULLBACK_WITNESS__W3_50_OSCILLON_CORE_SPECTRAL_GATE_PRESSURE_STRESS_COVARIANT_BACKREACTION_AND_UNIVERSALITY_OPEN`.

This closes the bounded reduced candidate and its falsifiers. Stop here. Do not open a particle catalogue, mass-ratio fit, Koide/C3 calculation, Planck hierarchy, full node action, compact-object model, cosmological refit, or intuitive-document edit inside W3-56.
