# W3-55: Relational Invariant Separation and Relative-Scale Reconstruction Audit

## Result

This stage resolves the search for a universal scale anchor without adding a
new microscopic ontology.

The present RefG chain contains two exact but physically different conserved
records:

1. On the ideal-comoving fixed-incidence branch, the relational path count
   between two selected bodies is fixed:

   `N_12(t)=N_12,0`.

2. On the selected neutral collective-phase branch, the Noether phase charge
   is fixed:

   `Q_C(t)=Q_C0`, with `Q_C/Q_C0=eta_F a^3=1`.

No existing RefG result identifies these quantities. In particular,

`Q_C=q_* N_12`

is neither assumed nor derived in W3-55. A path count and an integrated phase
charge are independent ledgers.

The exact universal result available on the selected homogeneous branch is a
dimensionless relative-scale reconstruction:

`eta_F = A^(-6/5)`,

`a = A^(2/5)`,

`p = A^(-3/5)`,

`P_F/P_F0 = A^(-6/5)`,

where `A=a/p` and the present normalization is
`a_0=p_0=A_0=eta_F0=1`.

Thus one operational scale history `A` fixes the normalized foundation scale,
material reference and pressure state on this branch. It does not fix an
absolute dimensional length such as `ell_F0`. The theory therefore supplies
an exact relative standard, not an external absolute ruler.

## Status

`PASS_EXACT_CONDITIONAL_INVARIANT_SEPARATION_AND_RELATIVE_SCALE_RECONSTRUCTION__COUNT_CHARGE_IDENTIFICATION_ABSOLUTE_SCALE_AND_COFRAME_MEASURE_MATCHING_OPEN`

## Target and stopping rule

The bounded target is:

`existing graph invariant + existing phase invariant
    -> prove that their roles remain separate
    -> reconstruct the selected normalized state from A
    -> determine whether an absolute dimensional scale follows
    -> STOP.`

This stage does not construct a node-scale action, derive graph dimension,
derive the collective phase current from individual nodes, change the W3-54
source action, fit observations, or open a particle calculation.

## 1. Registered inputs

No upstream artifact is modified by this stage.

| Input | Registered result used here | SHA-256 |
|---|---|---|
| W3-40 | Ideal-comoving `N_12` is fixed; `L_12=N_12 ell_F0 a`; `A=a/p` | `101F1AE30430D519307E390536C0E7C0D6EA740EAEEAAF7458B5BCD8EEF2B8D6` |
| W3-42 | Conditional three-dimensional homogeneous measure `V_a=a^3 V_0` | `4CC4674775525A3C76CD8CB282461E5E83B651AFF3554DE21983568EE7E1F9F1` |
| W3-46 | Material/reference response and one-effect bookkeeping | `0109ED3D5E8DAEC55DBD0F01F8B05932E6F653373438455C32A3D26378E0F3B2` |
| W3-47 preregistration | Selected `P_F/P_F0=eta`, `p^2=P_F/P_F0` and `A=a/p` coupling kernel | `ED1D5B6C2A982CDAAFD6739E6A8388219931300B8DF92BF015AB2112001049A5` |
| W3-47 verifier | Exact conditional coupling-kernel algebra and current W3-42 provenance policy | `D65D8644F443D7991FADBF5F808453B4B227FB87BCEF789A8E8C8F89860BFC1F` |
| W3-50 | `Q_C/Q_C0=eta_F a^3=1` on the selected neutral phase-density branch | `1CB66438A6BF53F1A661A014328204C05EDFE847F81D876DEFE69EAA400591DB` |
| W3-54 | Covariant phase current, one Hilbert source and the same homogeneous charge reduction | `6CC748EB806D0BCCAAF63105567A5D9B1569C56F6B53951C554EC4BAD1AA9879` |
| Current Georgian intuitive-manuscript snapshot | Relational measurement, fixed ideal-comoving count, one-event cosmological reading and exact relative-scale reconstruction | `172B8B94798B608A429C1CEFA6FA183578CFB12F080E510DA537F1D40E920C82` |

The hashes pin the exact evidence audited by this stage. A later upstream
change requires rerunning the verifier and reopening this contract.

## 2. Non-overlapping variable roles

| Symbol | Role |
|---|---|
| `N_12` | Fixed W3-40 path inventory under one registered relational-step/link counting convention for the selected ideal-comoving pair |
| `ell_F0` | Unmeasured reference length assigned to one foundation step |
| `L_12` | Foundation-side path length `N_12 ell_F0 a` |
| `n_C` | Proper density of organized neutral collective phase action in the active W3-50/W3-54 branch |
| `Q_C` | Integrated neutral Noether phase charge |
| `eta_F` | Normalized phase-action density `n_C/n_C0` |
| `a` | Normalized homogeneous foundation scale |
| `p` | Normalized material ruler/clock/mass response factor |
| `A` | Operational scale `a/p` |

Three distinctions are mandatory:

- `N_12` is not the total number of graph vertices in the universe.
- `N_12` is not `Q_C` or `Q_C/q_*`.
- `n_C` is not a node density.

The intuitive manuscript describes this same intervening path
ontologically as its foundation particles and bonds. W3-55 does not add a
particle count to a bond count and does not assert a new vertex--edge
identity. Its algebra uses only the single W3-40 path inventory `N_12`.

This stage introduces no replacement symbols for `eta_F` or `n_C` and does
not reclassify either active variable.

## 3. Exact graph invariant

For the selected ideal Hubble-flow pair, W3-40 holds the incidence/path record
fixed while the physical weight of each relational step changes:

`dN_12/dt=0`,

`ell_F(t)=ell_F0 a(t)`,

`L_12(t)=N_12 ell_F0 a(t)`.

This invariant is conditional on the declared pair and branch. Peculiar
motion, path reconnection, collisions, local gravitational displacement or a
change of graph representation can change the relevant path. It is therefore
a relational path invariant, not a universal domain inventory and not a
microscopic conservation law for every possible coarse graining.

The path lies in an already activated/correlated post-Genesis sector and is
registered relative to the existing `R_act` evolution. `N_12` is not a
Genesis-front count, and its conservation does not define the global birth
geometry or require a center of expansion.

## 4. Exact phase-charge invariant

The active W3-50/W3-54 branch has a common internal phase symmetry. Its
Noether current obeys the registered conservation law. For a homogeneous
comoving domain with no relative boundary flux,

`Q_C=constant`,

`Q_C/Q_C0=eta_F a^3=1`.

Consequently,

`eta_F=a^(-3)`.

This is dilution of conserved organized phase action over the selected
three-dimensional homogeneous measure. It is not a statement about the
number of foundation nodes.

## 5. Separation theorem

The two conservation statements do not imply a count-charge equality.

A fixed `N_12` permits different `Q_C` values because the collective phase
state or its normalization may differ while the same relational path remains.
Conversely, the same integrated `Q_C` can be represented on graph
descriptions with different path counts or resolutions.

Therefore neither

`Q_C=q_* N_12`

nor a universal vertex-count invariant follows from W3-40, W3-50 or W3-54.
Deriving such a relation would require a microscopic theorem that selects a
node ontology, a graph resolution, a charge quantum, one charge occupation
per registered node and a node-to-current continuum map. None of those
premises is needed for the result of this stage.

## 6. Relative-scale reconstruction theorem

Use only the already selected homogeneous relations

`eta_F a^3=1`,

`P_F/P_F0=eta_F`,

`p^2=eta_F`,

`A=a/p`.

The positive normalized branch gives

`p=a^(-3/2)`

and hence

`A=a^(5/2)`.

The inverse map is therefore unique on this branch:

`a=A^(2/5)`,

`p=A^(-3/5)`,

`eta_F=P_F/P_F0=A^(-6/5)`.

Every exponent is fixed by the registered cubic dilution law and the existing
material-response law. No fitted power, object-dependent parameter or second
cosmological factor is introduced.

This is the precise sense in which the selected RefG branch has a universal
scale formula: it reconstructs normalized ratios between foundation,
material and operational scales. It does not assign a dimensionful size to
the foundation from outside it.

## 7. Why no absolute dimensional ruler follows

A material reference length has the form

`ell_mat(t)=ell_mat0 p(t)`.

The directly meaningful ratio for the selected path is

`L_12/ell_mat
  = (N_12 ell_F0/ell_mat0) A.`

The evolution is fixed by `A`. The constant normalization
`N_12 ell_F0/ell_mat0` is not fixed by the dynamics above. A common change of
the reference length convention changes dimensional labels while leaving all
dimensionless comparisons unchanged.

Thus:

- `N_12` fixes a relational count, not the length of one step;
- `Q_C/Q_C0` fixes a conserved phase-charge ratio, not a meter;
- `A` fixes relative scale evolution, not an external absolute size.

The absence of an absolute dimensional ruler is the mathematical outcome of
the audit, not a missing algebraic step.

## 8. Conditional two-measure identity

For a generic conserved scalar `Q`, if one separately chooses two spatial
measures for the same reference coordinate cell,

`V_F=a^3 V_0`,

`V_A=A^3 V_0`,

and defines `n_F=Q/V_F` and `n_A=Q/V_A`, then algebra alone gives

`n_A=p^3 n_F`.

This is a conditional spatial Jacobian. It is not used in the proof above. It
does not relabel the active W3-50/W3-54 `n_C`, does not create a second source,
and does not establish a covariant map between the W3-54 coframe measure and
an independently postulated `A`-metric measure. That coframe/operational
measure matching remains a separate microscopic-continuum question.

## 9. One causal event, not two additive effects

The reconstruction preserves the established one-effect reading:

`a increases
  -> eta_F and P_F/P_F0 decrease
  -> p decreases
  -> the internal observer reads A=a/p.`

Foundation expansion and material-reference reduction are two linked
descriptions inside this single causal chain. They are not independent
factors to be added or multiplied again in an observable.

## 10. Disposition of the preliminary W3-55 draft

The preliminary draft is rejected and replaced by this audit. The following
draft claims are not retained:

- a universal conserved vertex inventory `N_C`;
- a unit-weight identity `Q_C=q_*N_C`;
- renamed state variables `Xi_N` and `chi_F`;
- a reinterpretation of active `n_C` as an `A`-measure density;
- a covariant `A`-metric current or Hilbert-source map;
- the claim that a conserved count supplies an absolute or universal
  dimensional anchor.

No downstream result depends on those draft claims.

## 11. Claim contract

- `CLAIM_ID`:
  `W3_55_RELATIONAL_INVARIANT_SEPARATION_RELATIVE_SCALE_RECONSTRUCTION`.
- `CLAIM`: On the registered ideal-comoving branch, `N_12` is a fixed
  relational path count. Independently, on the selected neutral
  collective-phase branch, `Q_C` is conserved and
  `eta_F a^3=1`. The existing response laws then give the exact normalized
  reconstruction
  `a=A^(2/5)`, `p=A^(-3/5)` and
  `eta_F=P_F/P_F0=A^(-6/5)`. No count-charge equality or absolute
  dimensional ruler follows.
- `TYPE`:
  `EXACT_ALGEBRAIC_RECONSTRUCTION_AND_LEDGER_SEPARATION_ON_DECLARED_CONDITIONAL_BRANCHES`.
- `MODEL_VERSION`: `W3-FOUNDATION-v2.0-RELATIVE-SCALE-AUDIT`.
- `ASSUMPTIONS`:
  1. The W3-40 ideal-comoving fixed-path branch.
  2. The W3-42 selected three-dimensional homogeneous measure.
  3. The W3-50/W3-54 neutral phase-current branch with no relative charge
     flux through the selected comoving boundary.
  4. The inherited selected laws `P_F/P_F0=eta_F` and `p^2=eta_F`.
  5. Positive normalized scales and
     `a_0=p_0=A_0=eta_F0=1`.
- `DOMAIN`: Homogeneous post-Genesis branch and ideal-comoving relational
  paths; normalized positive scale ratios.
- `CONVENTIONS`: `A=a/p`; present-epoch normalization; `n_C` and `Q_C` retain
  their active W3-50/W3-54 meanings; no external dimensional ruler.
- `FREEDOM_LEDGER`: No new fitted parameter, exponent, charge quantum, node
  occupation, graph resolution or absolute reference length is introduced.
- `DEPENDENCIES`: W3-40, W3-42, W3-46, W3-47, W3-50, W3-54 and the current
  Georgian intuitive-ontology snapshot at the hashes registered in Section 1.
- `METHOD`: Role audit, symbolic exponent inversion, mutation/counterexample
  checks and provenance-locked regression.
- `PASS_CONDITION`: All four selected branch identities are mutually
  consistent; the inverse powers of `A` are exact; `N_12` and `Q_C` remain
  distinct; no active density or source is relabelled; no absolute scale is
  claimed.
- `FAIL_CONDITION`: Any hidden `Q_C=q_*N_12` identification, universal vertex
  count, second cosmological factor, `n_C` measure reclassification, fitted
  exponent, or absolute dimensional normalization enters the result.
- `FALSIFIER`: The selected branch fails if an independently defined
  dimensionless readout of `a`, `p` or `P_F/P_F0` violates the stated powers
  of `A` under the same domain and normalization, or if the registered
  current has a nonzero unledgered source/flux.
- `RESIDUAL`: Zero for the exact symbolic identities. No observational
  residual is evaluated at this stage.
- `ERROR_BOUND`: Algebraic error is exactly zero. No numerical
  coarse-graining approximation is introduced. Physical extrapolation
  outside the declared branches is not licensed.
- `VALIDITY_HEALTH`:
  `ALGEBRAICALLY_HEALTHY_ON_PINNED_BRANCH__DYNAMICAL_STABILITY_CAUSALITY_AND_MICROSCOPIC_HEALTH_INHERITED_NOT_RECHECKED`.
- `BRANCHES`: Positive normalized homogeneous density branch; ideal-comoving
  fixed-path branch. Local motion, graph reconnection, inhomogeneous
  transport and pre-Genesis geometry are excluded.
- `OBSERVABLE_MAP`: `A` is the operational input state coordinate of this
  bounded reconstruction. W3-55 maps a supplied `A` to normalized `a`, `p`
  and `P_F/P_F0` on the selected branch; it does not rederive or test the
  separate observational realization of `A`.
- `FORWARD_MODEL`: Given `A>0`, return
  `(a,p,eta_F,P_F/P_F0)=(A^(2/5),A^(-3/5),A^(-6/5),A^(-6/5))`.
- `DATA_ROLE`: `NO_DATA_READ_NO_DATA_FIT`.
- `IDENTIFIABILITY`: The normalized decomposition is unique only after the
  selected closure and present normalization are imposed. The dimensional
  constants `ell_F0` and `ell_mat0` remain unidentifiable from `A`.
- `BENCHMARK`: Reproduces W3-40's `A=a/p` dictionary and W3-50/W3-54's
  `eta_F a^3=1` reduction without changing either upstream result.
- `CLOSURE_FLAGS`:
  - `W3_40_IDEAL_COMOVING_N12_CONSERVED=true`
  - `W3_50_W3_54_PHASE_CHARGE_CONSERVED_ON_SELECTED_BRANCH=true`
  - `N12_AND_QC_ROLES_SEPARATED=true`
  - `A_TO_a_RELATIVE_RECONSTRUCTION_EXACT_ON_SELECTED_DENSITY_BRANCH=true`
  - `TWO_MEASURE_SPATIAL_JACOBIAN_CONDITIONAL_EXACT=true`
  - `ONE_SOURCE_LEDGER_UNCHANGED=true`
  - `NO_ABSOLUTE_RULER_DERIVED=true`
  - `DOMAIN_VERTEX_COUNT_UNIVERSAL_DERIVED=false`
  - `N12_EQUALS_QC_OVER_QSTAR_DERIVED=false`
  - `UNIT_WEIGHT_SECTOR_DERIVED=false`
  - `PREGEOMETRIC_GRAPH_DERIVED=false`
  - `NODE_TO_PHASE_CURRENT_DERIVED=false`
  - `NODE_TO_COFRAME_CONTINUUM_DERIVED=false`
  - `ACTIVE_NC_A_MEASURE_RECLASSIFICATION_DERIVED=false`
  - `COFRAME_a_TO_OPERATIONAL_A_MAP_DERIVED=false`
  - `ABSOLUTE_ELL_F0_DERIVED=false`
- `CROSSCHECK`: The verifier checks dependency hashes, all exact powers,
  path-ratio rescaling invariance, count-charge counterexamples, the
  conditional spatial Jacobian and forbidden draft-claim markers.
- `PROVENANCE`: Immutable upstream SHA-256 records are pinned in Section 1.
  The final W3-55 paths and two downstream insertions are listed below; no
  Git commit or publication claim is made by this uncommitted stage.
- `FILES`:
  1. `w3_55_relational_invariant_separation_relative_scale_contract.md`
  2. `w3_55_verify_relative_scale_reconstruction.py`
  3. `../Cosmology_and_LSS/README.md` (status integration only)
  4. `../Lagrangian_Formulation/RefG_Formal_Proof.md` (downstream audit only)

## Final decision

RefG has an exact universal **relative** scale reconstruction on the selected
homogeneous branch. It has no derived absolute dimensional ruler. The
relational path count and neutral phase charge remain two independent
invariants, exactly as required by the existing ontology and source ledger.

The stage stops at this boundary. A future microscopic project may attempt a
node-to-current or node-to-coframe theorem; W3-55 neither assumes its answer
nor needs it for the result above.
